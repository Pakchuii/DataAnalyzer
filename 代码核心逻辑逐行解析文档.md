# 集成统计分析与可视化系统：核心源码逐行解析

本文档针对毕业设计《集成统计分析与可视化的表单数据处理系统》的**后端算力层**及**前端状态管理**等核心逻辑提供了详细的“逐行级”解析。配合答辩 PPT 食用最佳。

---

## 一、 后端算法核心层解析

### 1. 机器学习：集成多维惩罚衰减机制的预测推理 (详见 `service_ml.py`)
系统的 AI 预测没有盲从 Sklearn 返回的分数，而是编写了一套“启发式惩罚阻断机制”。

```python
# —— 截取自 backend/services/service_ml.py 中的 do_predict_new() 函数 ——

# 1. 提取测试集进行模型预测，用于后续展示
sample_size = min(50, len(X_test))                     # 限制最大仿真输出条数为50，防止前端崩盘
X_show = X_test.iloc[:sample_size]                     # 提取特型矩阵输入
y_show_real = y_test.iloc[:sample_size].values         # 提取对应的真实目标值
y_show_pred = model.predict(X_show)                    # 通过上文已训练的 RandomForest 模型进行输出推理

# ====== 🚀 核心算法创新：引入启发式多维惩罚机制 ======
# 2. 计算基准指标
sample_r2 = r2_score(y_show_real, y_show_pred)         # 计算统计学上的经典 R² 决定系数
std_pred = np.std(y_show_pred)                         # 计算预测序列的标准差
std_real = np.std(y_show_real)                         # 计算真实序列的标准差

# 3. 计算 Pearson 趋势相关系数（侦测拟合曲线相位是否一致，而非单纯的数值贴合）
if std_pred == 0 or std_real == 0:
    corr = 0                                           # 防止完全常数预测导致的除0异常
else:
    corr = np.corrcoef(y_show_real, y_show_pred)[0, 1] # 提取皮尔逊相关系数矩阵的 [0,1] 关联值

# 4. 平均绝对百分比误差 (MAPE) 推导基础准确率
# 加分母 1e-9 的平滑项是为了避免真实值为 0 时除数为 0 导致 Infinity
mape = np.mean(np.abs((y_show_real - y_show_pred) / (y_show_real + 1e-9))) 
acc = max(0, 1 - mape)                                 # 将误差转算为 0到1 之间的基础准确率权重

# 5. 退化熔断机制：针对劣质拟合实施“无情镇压”
# 若 R2 低于0.15 或 曲线趋势背离（corr<0.3），说明模型几乎退化为出一条“纯均值直线”
if sample_r2 < 0.15 or corr < 0.3:
    confidence = round(acc * 45, 2)                    # 【严重惩罚】：极限分数压制在 45 分以内，拒绝误导小白用户
else:
    # 正常拟合情况：融合基础准确率(40%) 与 曲线形状贴合度(60%) 生成最终置信度
    confidence = round((acc * 0.4 + corr * 0.6) * 100, 2)

if confidence > 99:
    confidence = 98.75                                 # AI领域没有100%绝对，保留敬畏数值
```

---

### 2. 三阶段自适应数据清洗管道 (详见 `process_routes.py`)
系统摒弃单一判断法则，根据 N 和偏度自动路由不同的切割面去拦截离群点。

```python
# —— 截取自 backend/routes/process_routes.py 中的 smart_clean_series() ——

def smart_clean_series(s):
    s_valid = s.dropna()                             # 第一步清洗当前列的 NaN 空值
    n = len(s_valid)                                 # 获取该特征列的绝对样本数

    if n < 3:
        return s, "样本极小(跳过)", 0                   # 样本不足3条，统计无意义，直接放行

    # 【第一阶】：小样本防御机制 (N < 30) - 选用 Dixon's Q 检验
    if n < 30:
        q_crit = q95.get(n, 0.260)                   # 查表获取对应 n 的 95% 置信度 Q值临界
        s_sorted = s_valid.sort_values().values      # 对样本进行排序，去寻找数据“断崖点”
        s_clean = s.copy()                           # 拷贝原始数据结构用于回填
        outliers_count = 0                           # 初始化异常剔除数
        
        range_all = s_sorted[-1] - s_sorted[0]       # 获取样本级差 (Max-Min)
        if range_all > 0:
            # 检验下限断崖
            if (s_sorted[1] - s_sorted[0]) / range_all > q_crit:
                s_clean = s_clean.replace(s_sorted[0], s_sorted[1]) # 利用最近邻截断法修复底层离群点
                outliers_count += 1
            # 检验上限断崖（二次更新级差后）
            s_sorted_new = s_clean.dropna().sort_values().values 
            range_new = s_sorted_new[-1] - s_sorted_new[0]
            if range_new > 0 and (s_sorted_new[-1] - s_sorted_new[-2]) / range_new > q_crit:
                s_clean = s_clean.replace(s_sorted_new[-1], s_sorted_new[-2]) # 修复上层离群点
                outliers_count += 1
        return s_clean, "Dixon's Q检验 (N<30)", outliers_count

    else:
        # 【第二与第三阶并联】：大样本 (N >= 30) 引入 Skewness 偏度进行高阶动态路由
        skewness = s_valid.skew()                    # 计算偏度系数

        # 偏度绝对值 <= 1.0 通常被认为数据形态相对对称/近似正态
        if pd.notna(skewness) and abs(skewness) <= 1.0:
            # 【第二阶】：近似正态分布 - 3σ 准则 (数学效率最高)
            mean_val = s_valid.mean()                # 均指
            std_val = s_valid.std()                  # 标准差
            lower_bound = mean_val - 3 * std_val     # μ - 3σ 底部阻断墙
            upper_bound = mean_val + 3 * std_val     # μ + 3σ 顶部阻断墙
            
            # 统计超越墙内边界的坏账节点数量
            outliers_count = int(((s_valid < lower_bound) | (s_valid > upper_bound)).sum())
            s_clean = s.clip(lower=lower_bound, upper=upper_bound) # pd.clip 做标量一刀切限幅修复
            return s_clean, "3σ边界拦截 (N>=30, 呈近似正态)", outliers_count

        else:
            # 【第三阶】：严重偏态分布 - IQR 箱线图法 (非参数模型，极度稳健抗偏态)
            Q1 = s_valid.quantile(0.25)              # 计算前 1/4 位置的 25% 极值位
            Q3 = s_valid.quantile(0.75)              # 计算后 3/4 位置的 75% 极值位
            IQR = Q3 - Q1                            # 计算二者之间的四分位距箱体长度
            lower_bound = Q1 - 1.5 * IQR             # 下胡须阻断线
            upper_bound = Q3 + 1.5 * IQR             # 上胡须阻断线

            outliers_count = int(((s_valid < lower_bound) | (s_valid > upper_bound)).sum())
            s_clean = s.clip(lower=lower_bound, upper=upper_bound) # 将偏态离群值压缩到胡须内
            return s_clean, "IQR箱线图法 (N>=30, 呈偏态分布)", outliers_count
```

---

### 3. 数据安全探针与掩码洗白 (详见 `service_security.py`)
扫描用户表单，将可能泄露的信息进行基于字符串长度的不可倒推清洗。

```python
# —— 截取自 backend/services/service_security.py 中的 do_mask() 函数 ——

def mask_val(x):
    """长度感知型不可逆混淆算法"""
    x = str(x).strip()                                    # 强转并切除多余空白
    if x in ['nan', 'None', '']: return x                 # 绕过系统占位符
    
    # 策略路由与字符级掩码涂黑
    if len(x) <= 2: 
        return x[0] + '*'                                 # “王冰” -> “王*”
    elif len(x) == 3: 
        return x[0] + '*' + x[-1]                         # “徐志摩” -> “徐*摩”
    else: 
        return x[:4] + '****'                             # 高危长串例如身份证:“3204****”

# 依托特征循环完成自动嗅探扫描
for col in df.columns:
    # Any() 函数配合迭代器寻找高敏关键词。利用 lower() 避免大写绕过防御
    if any(kw in str(col).lower() for kw in ['名', 'name', '号', 'id', '手机', '电话', '身份']):
        masked_cols.append(col)                           # 将此危类装入被修改状态池
        df[col] = df[col].apply(mask_val)                 # Pandas 向量化apply 挂载遮掩算法全量洗白
```

---

### 4. t 检验的 Bootstrap 防爆机制 (详见 `service_stats.py`)
防止客户输入的数据因为样本量或者严重异常的正态性校验失败造成的检验断点。

```python
# —— 截取自 backend/services/service_stats.py 中的 do_ttest() 函数 ——

# T检验对双样本必须要求一定的正态分布稳定性，预先计算长度
n1, n2 = len(d1), len(d2)

# 当任意某组处于小样本范围时激活独立校验保护 (通常大样本下T检鲁棒性好)
if n1 < 30 or n2 < 30:
    # 只有当样本 >= 3 时，利用 scipy.stats.shapiro 跑正态校验提取 p 值
    p_shap1 = stats.shapiro(d1)[1] if n1 >= 3 else 0
    p_shap2 = stats.shapiro(d2)[1] if n2 >= 3 else 0
    
    # 【核心容错逻辑】：一旦发现原本样本就不正态 (P<0.05 拒绝假设)
    if p_shap1 < 0.05 or p_shap2 < 0.05:
        # 激活大数定理！在前端毫无察觉情况下替换底座阵列。
        # 调用 random.choice 原样池进行 size=100 的放回式“虚拟扩容重抽样”。
        # 此时样本不仅增容，且符合中心极限定理回归正态
        d1 = np.random.choice(d1, size=100, replace=True) 
        d2 = np.random.choice(d2, size=100, replace=True)
        # 向前端操作台日志注入预警文字
        algorithm_note = "数据非正态, 已触发 Bootstrap 重抽样(N=100)"

# 执行健壮的韦尔奇(Welch's t-test，方差不齐默认 True 兼容)
stat, p = stats.ttest_ind(d1, d2, equal_var=False) 
```

---

## 二、 前端 Vue.js 数据管控层解析

### 1. 安全沙盒与跨平台 CSV 防乱码出库
针对前端原生的浏览器导致输出至 Excel 时产生的乱码截断，做了底层的魔改适配。

```javascript
// —— 截取自前端的 DataEditor.vue，导出逻辑部分 ——
const handleExport = () => {
  // 1. 将数据列表转储，用 "\n" 拼接，同时列属性之间用 "," 分隔
  const csvContent = headers.value.join(",") + "\n" + tableData.value.map(r => Object.values(r).join(",")).join("\n");
  
  // 2. 核心补丁：原生强加 \uFEFF 给浏览器解码器喂入 UTF-8 BOM 标记，根除 Windows Excel 接盘时的乱字符痛点
  const BOM = "\uFEFF"; 
  // 3. 构建 Blob 对象，标记 MIME 类型为 text/csv
  const blob = new Blob([BOM + csvContent], { type: "text/csv;charset=utf-8;" });
  
  // 4. 沙盒导出隔离操作，构建 a 标签虚拟下载链接
  const link = document.createElement("a");
  const url = URL.createObjectURL(blob);
  link.setAttribute("href", url);
  // 保留用户操作特征记录
  link.setAttribute("download", `导出_${new Date().getTime()}.csv`);
  link.style.visibility = "hidden";
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
}
```

### 2. Memento 备忘录模式的撤销栈 (Undo Stack) 机制

在网页进行数万格阵的深拷贝撤回极易因为 V8 引擎配置瓶颈导致“OOM Out of memory”。

```javascript
// —— 截取自前端 store.js 或对应封装中的逻辑机制 ——

// 定义当前工作数据副本快照对象
let state_tree = { current: [...data] } 
// Memento 全局备忘记录栈
let history_stack = []

function saveSnapshot() {
  // 1. 设置截断阀门，限定只保存前30次的编辑过程，这决定了内存空间的稳定
  if (history_stack.length >= 30) {
      history_stack.shift(); // FIFO：抛弃最远古的历史
  }
  // 2. 将当前的数据进行深度解构序列化后，推入历史缓存链表
  history_stack.push(JSON.parse(JSON.stringify(state_tree.current)));
}

// 触发用户 Ctrl+Z 或按钮
function undoOperation() {
  if (history_stack.length > 0) {
      // 3. pop 顶部最近栈出列，反向还原替换屏幕的 current 展示视图
      state_tree.current = history_stack.pop();
      console.log("[Log] 已执行基于备忘录模式的回退操纵！");
  } else {
      console.warn("历史底线达到，无法继续退卷。");
  }
}
```
*以上核心源码解构全量对齐课题论文实现功能，供各位老师查卷参考。*
