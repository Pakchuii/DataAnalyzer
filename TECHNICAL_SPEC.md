# 📑 DataAnalyzer Pro: 算法技术白皮书

> 「 每一个数字背后，都藏着一段等待被讲述的故事 」

本系统集成了一套严谨的科学统计与机器学习引擎，旨在为实验数据提供具备统计学显著性的分析结论。

---

## ✦ 1. 智慧数据治理引擎 (Smart Cleaning)

系统采用 **「三阶自适应路由」** 策略，根据样本量 N 与分布偏度自动选择最优清洗算法。

### 路由决策树

```
输入数据集
    │
    ├── N < 30 ──────────→ 🔬 Dixon's Q 检验
    │
    └── N ≥ 30
         │
         ├── |Skewness| ≤ 1.0 ──→ 📐 3σ 准则 (正态假设)
         │
         └── |Skewness| > 1.0 ──→ 📦 IQR 箱线图法 (非参数)
```

### 第一阶：Dixon's Q 检验 (N < 30)

针对实验室常态小样本数据：

$$Q = \frac{|X_{outlier} - X_{neighbor}|}{Range}$$

- 置信度：默认 P=0.95 的临界值表
- 适用场景：精密仪器的少量重复实验

### 第二阶：三倍标准差准则 (N ≥ 30, 近似正态)

当偏度绝对值 ≤ 1.0 时，系统判定为近似正态分布：

$$\text{保留范围} = \mu \pm 3\sigma$$

- 处理方式：边界裁剪 (Clipping)，消除离群值但保留样本总量

### 第三阶：IQR 箱线图法 (N ≥ 30, 偏态分布)

针对长尾分布等偏态明显的业务数据：

$$\text{下界} = Q_1 - 1.5 \times IQR \quad|\quad \text{上界} = Q_3 + 1.5 \times IQR$$

- 优势：对异常值极度稳健，不依赖分布假设

---

## ✦ 2. 统计推断引擎 (Statistical Inference)

### Welch's t-test（不等方差纠偏）

系统默认放弃经典 Student's t-test，采用更稳健的 Welch 修正版本：

- **问题**：当两组数据方差不相等时，经典 t-test 会导致 I 类错误膨胀
- **解决**：Welch's t-test 使用 Satterthwaite 公式动态修正自由度

$$df = \frac{\left(\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}\right)^2}{\frac{(s_1^2/n_1)^2}{n_1-1} + \frac{(s_2^2/n_2)^2}{n_2-1}}$$

### Bootstrap 1000 次重采样

为增强检验鲁棒性，系统引入蒙特卡洛重采样技术：

1. 在原始样本中有放回抽样 1000 次
2. 构建经验分布函数
3. 计算更精确的 95% 置信区间

---

## ✦ 3. 机器学习预测系统 (AI Inference)

### 随机森林回归 (Random Forest Regression)

```
原始数据 → 特征自动检测 → 标准归一化 → 模型训练 → 推理输出
                                            │
                                    ┌───────┴───────┐
                                    │               │
                              R² 优度评分    Gini 特征重要性
                                    │               │
                              置信度惩罚      特征贡献排名
```

- **特征工程**：自动检测数值列，标准化处理
- **权重分解**：通过 Gini Importance 实时输出特征贡献度排名
- **置信度惩罚**：当 R² 偏低时自动降低推理信心值，防止"过度自信"

### AI 叙述性摘要

系统基于统计元数据自动生成结构化文本报告：
- 自动识别异常指标并高亮
- 将复杂统计量转化为业务语言

---

## ✦ 4. 数据安全与导出

### 数据脱敏 (Masking)
- 支持列级 SHA-256 哈希脱敏
- 敏感字段自动检测与替换

### 高保真报表导出
- **PNG 截图**：基于 HTML2Canvas 的 Canvas 像素级捕获
- **PDF 编排**：使用 jsPDF 实现多维图表与解读文本的矢量排版

---

<p align="center">
  <sub>✦ 数据不说谎，算法不偏见 ✦</sub>
</p>
