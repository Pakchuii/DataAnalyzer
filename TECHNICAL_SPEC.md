# 📑 DataAnalyzer Pro: 算法技术白皮书

本系统集成了一套严谨的科学统计与机器学习引擎，旨在为实验数据提供具备统计学显著性的分析。

---

## 1. 智慧数据治理引擎 (Smart Cleaning)

系统采用 **“三阶自适应路由 (Three-Tier Routing)”** 策略，根据样本量 $N$ 与分布偏度自动执行最优清洗算法。

### 第一阶：Dixon's Q 检验 ($N < 30$)
主要针对实验室中的常态小样本数据：
- **算法逻辑**: 检验样本序列中极大值或极小值与相邻值的差距。
- **判定模型**: 
    $$Q = \frac{|X_{outlier} - X_{neighbor}|}{Range}$$
- **置信度**: 默认采用 $P=0.95$ 的临界值表。

### 第二阶：近似正态三倍标准差准则 ($N \ge 30, |\text{Skewness}| \le 1.0$)
当偏度绝对值小于 1.0 时，系统判定为近似正态分布：
- **公式**: $\mu \pm 3\sigma$
- **处理方式**: 强制边界拦截 (Clipping)，既消除了离群值干扰，又保留了样本总量。

### 第三阶：非参数 IQR 箱线图法 ($N \ge 30, |\text{Skewness}| > 1.0$)
针对偏态明显的实际业务数据（长尾分布）：
- **判定界限**: 
    - 下界: $Q1 - 1.5 \times IQR$
    - 上界: $Q3 + 1.5 \times IQR$
- **优势**: 对异常值极度稳健，不依赖总体分布假设。

---

## 2. 科学统计推断 (Statistical Engine)

### Welch's t-test (不等方差纠偏)
在进行组别差异比较时，系统默认放弃经典的 Student's t-test，采用更稳健的 Welch's t-test：
- **应用场景**: 处理两组数据方差不相等的情况，避免由于异方差性导致的 I 类错误膨胀。
- **自由度修正**: 采用 Satterthwaite 近似公式动态计算自由度。

### Bootstrap 1000次重采样
为了增强检验的鲁棒性，系统在统计核心中引入了重采样技术：
- **过程**: 在原始样本中进行 1000 次带放回抽样。
- **目标**: 构建经验分布函数，从而获得更精确的置信区间。

---

## 3. 机器学习预测系统 (AI Inference)

### 随机森林回归 (Random Forest Regression)
- **特征工程**: 自动检测数值列，进行标准归一化处理。
- **权重分解 (Gini Importance)**: 通过计算特征节点的不纯度减少量，实时输出特征贡献度排名。
- **置信度评价**: 除了 $R^2$ 优度，系统还结合了残差分布情况输出“推理信心值”。

---

## 4. 报表生成规范
- **高保真导出**: 基于 HTML2Canvas 的 Canvas 捕获技术。
- **PDF 编排**: 使用 JSPDF 实现多维图表与解读文本的矢量级排版同步。
