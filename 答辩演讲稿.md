# 毕业设计答辩演讲稿

各位评委老师好，我是软件工程专业的毛旭锋，今天我答辩的课题是**《集成统计分析与可视化的表单数据处理系统》**。我的指导老师是严然辉老师。以下是我的答辩汇报。

---

## 01. 课题背景 (对应 PPT 第 2 - 7 页)

### 【演讲内容】
首先是课题背景。在海量表单数据爆发的当下，非专业用户往往面临“有数据、无算力”的尴尬困境：如果是传统的表格软件处理十万级数据，极易发生内存溢出和程序假死；而专业的分析工具（如 SPSS 或 Python 脚本）又有较高的代码和统计学门槛。
对比现有方案，我们会发现国外的 Looker 或 Tableau 虽然强大，但存在高昂的授权费和复杂的部署要求；国内开源的 ECharts 等通常专注于可视化，缺乏从数据录入到最终诊断的完整生命周期。
因此，本系统应运而生。我的目标是打造一个**“低门槛、功能完整、高性能”**的数据分析平台。系统采用“零代码”的纯图形化交互，让数据从导入清洗、假设检验再到机器学习预测实现完整闭环。同时，为了避免“假死”，系统重构了底层渲染管线，确保在处理海量表单时依然保持高帧率流畅。

---

## 02. 技术基础与系统架构设计 (对应 PPT 第 8 - 14 页)

### 【演讲内容】
第二部分是系统的技术底座与架构设计。系统中数据的流转分为四大核心节点：“无感采集 ➔ 智能分析 ➔ 高帧率可视化 ➔ 沙盒化管理”。
为实现高并发与算力扩展引擎，我采用了严格的 B/S 前后端分离架构方案：
1. **用户展示层与前端业务层**：前端采用 Vue 3.0 进行构建，搭载 Pinia 进行状态管理，并使用 ECharts 引擎呈现高帧率数据大屏。
2. **中间通信路由**：采用 Axios 配合代理进行路由转发与跨域穿透。
3. **后端核心算力层**：以轻量级的 Python Flask 为敏捷微服务基座，深度驱动 Pandas 和 Scikit-learn 进行矩阵运算和机器学习推断。

本系统所依赖的底层算法非常严谨。例如，对于数据清洗，系统设计了包含 Dixon's Q、3σ 准则与 IQR 箱线图的三阶段自适应策略；对于假设检验采用了 Welch's t 检验配合 Bootstrap 重抽样容错机制；在机器学习上引入了带惩罚机制的随机森林模型。

---

## 03. 核心功能实现及代码解析 (对应 PPT 第 15 - 27 页)

在此部分，我将不仅展示系统实现了哪些功能，还会深入到后端底层，为大家剖析支撑这些能力的核心代码。

### 1. 自适应三阶段数据清洗 (PPT 17 页)
普通的系统在面对异常值时只有简单粗暴的截断，而我编写了“三阶自适应智能清洗引擎”。
**逻辑解析：**
* 第一阶段：针对 `N < 30` 的极小样本，启用 Dixon's Q 检验。
* 第二阶段：针对 `N ≥ 30` 且偏度形态正常的近似正态分布，数学效率最高的 3σ 边界准则被激活。
* 第三阶段：针对严重偏态的数据，引入 IQR（四分位距）箱线图法来进行切割。

**【核心代码展示】**
```python
def smart_clean_series(s):
    s_valid = s.dropna()
    n = len(s_valid)

    # 第一阶段：小样本 Dixon's Q 检验
    if n < 30:
        q_crit = q95.get(n, 0.260)
        # 寻找极值断崖并剔除...
        return s_clean, "Dixon's Q检验 (N<30)", outliers_count

    else:
        # 引入偏度形态探针判断分布
        skewness = s_valid.skew()

        if pd.notna(skewness) and abs(skewness) <= 1.0:
            # 第二阶段：3σ 边界拦截
            lower_bound = mean_val - 3 * std_val
            upper_bound = mean_val + 3 * std_val
            s_clean = s.clip(lower=lower_bound, upper=upper_bound)
            return s_clean, "3σ边界拦截", outliers_count

        else:
            # 第三阶段：严重偏态分布使用 IQR 箱线图法
            Q1, Q3 = s_valid.quantile(0.25), s_valid.quantile(0.75)
            IQR = Q3 - Q1
            s_clean = s.clip(lower=Q1 - 1.5 * IQR, upper=Q3 + 1.5 * IQR)
            return s_clean, "IQR箱线图法", outliers_count
```

### 2. Z-score 和数据隐私脱敏 (PPT 18 页)
数据合规性是重中之重。系统会在后端进行自动特征匹配，拦截类似“姓名”、“手机”等高危属性列，自动实施“长度感知型的不可逆混淆掩码”脱敏计算。

**【核心代码展示】**
```python
def mask_val(x):
    # 长度感知混淆算法
    if len(x) <= 2: return x[0] + '*'
    elif len(x) == 3: return x[0] + '*' + x[-1]
    else: return x[:4] + '****'

for col in df.columns:
    # 自动探测高危列并触发洗白机制
    if any(kw in str(col).lower() for kw in ['名', 'name', '手机', '身份证']):
        df[col] = df[col].apply(mask_val)
```

### 3. T 检验与 Bootstrap 容错 (PPT 20 页)
T检验面临的核心阻碍是使用者不清楚自己的数据是否符合“正态性分布要求”。系统如果遇到非正态数据盲目执行，会让用户陷入报错死锁。因此我写了重载拦截：一旦 `Shapiro-Wilk` 校验失败，立刻激活基于大数定律的 100 次 `Bootstrap` 重置抽样，保障测试顺利下发。

**【核心代码展示】**
```python
# 当样本容量不足时，触发独立正态性校验
if n1 < 30 or n2 < 30:
    p_shap1 = stats.shapiro(d1)[1] if n1 >= 3 else 0
    if p_shap1 < 0.05:
        # 正态校验破产，强行切入 Bootstrap 有放回抽样 (N=100) 扩容
        d1 = np.random.choice(d1, size=100, replace=True)
        algorithm_note = "数据非正态, 已触发 Bootstrap 重抽样(N=100)"
```

### 4. 机器学习：集成了多维多级惩罚机制的预测引擎 (PPT 21 页)
在这个模块中，我使用了 `RandomForest`（随机森林）来完成预测，重点是不盲目信服模型结果。后端在出图前，会计算 R² 拟合度和特征相位相关性。若模型退化为纯均值拟合，系统将主动通过内置的启发式惩罚系数去“粉碎”置信度分数，避免错误引导用户决策。

**【核心代码展示】**
```python
# 核心算法创新：引入启发式多维惩罚机制
sample_r2 = r2_score(y_show_real, y_show_pred)
corr = np.corrcoef(y_show_real, y_show_pred)[0, 1]
mape = np.mean(np.abs((y_show_real - y_show_pred) / max(y_show_real, 1e-9))) # 避免除 0 异常
acc = max(0, 1 - mape)

# 退化熔断：若 R2 极低或相关性破灭，触发无情惩罚机制压低置信度
if sample_r2 < 0.15 or corr < 0.3:
    confidence = round(acc * 45, 2)
else:
    confidence = round((acc * 0.4 + corr * 0.6) * 100, 2)
```

### 5. 系统的数据兜底机制：沙盒隔离与防呆设计 (PPT 23 - 27 页)
为了让系统变得“小白也安全”（防呆设计）：
* **CSV 导出**：我在应用层向数据流头部注入了原生 `\uFEFF` (UTF-8 BOM)，从根本上消除了 CSV 用 Excel 打开经常出现的乱码痛点。
* **物理级隔离**：无论用户怎么操作，系统强制划定 `outputs` 沙盒目录，并实施 `Overwrite Confirmed` 参数保护器，任何覆写数据的请求必须触发二次确认阻断，保护原始资产。
* **Mmento 备忘录模式**：面对万亿级网格表格撤回会导致显存打满溢出的缺陷，系统在前端设立了 `Memento` 设计模式的撤回状态栈，并施加了严格的顶限封锁机制（30层阻拦覆盖）。

---

## 04. UI及部署体验 (对应 PPT 第 28 - 32 页)

### 【演讲内容】
最后是界面交互与部署体验。前端采用 `Glassmorphism` (毛玻璃拟态) 风格设计，利用 `z-index` 的绝对层进行弹框阻塞及全局色彩化操作日志警报显示。分析视图和编辑视图之间无缝切换。而在个人化设计上可以实现底色皮肤热更操作。

针对部署与未来展望：
用户不仅可以将系统部署于云端的 Nginx 与 Gunicorn 并发式微服务集群中提供大容量请求；同时针对桌面用户，系统运用前后端脱水打压缩容工程将整个平台打包成了离线的跨平台单体 `.exe` 执行文件，做到即插即用毫无学习负担。

### 结尾致辞 (PPT 第 33 页)
我的汇报完毕，诚挚感谢各位老师的听取与批评指导！
