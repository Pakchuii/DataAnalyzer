# 全系统核心架构：前后端代码独立模块与UI美学深度解构

本文档是对您整个工程各个独立的代码模块（Frontend + Backend）进行大卸八块的拆解说明，特别补充了针对 **UI 渲染及自定义皮肤引擎**的详细技术原理。

---

## 🎨 第一篇：前端 UI 设计与自定义皮肤引擎实现

系统最大的视觉亮点是**Glassmorphism（毛玻璃拟态）**风格。为了实现“深邃、呼吸、半透明”的极客科技质感，您的代码在表现层下足了功夫。

### 1. 动态自适应全画幅背景池 (`App.vue` 引擎拦截)
系统不仅支持普通的图片，还支持本地甚至服务器下发的 `.mp4` 高帧率视网膜视频作为底层背景，并在页面挂载时动态注入深浅遮罩。
```html
<!-- 这个区域负责承载“动态底图” -->
<div class="video-background">
  <!-- 当监测到系统处于原生背景模式时，拦截到黑白夜模式切换 -->
  <video v-if="store.bgType === 'default'" :key="store.isDarkMode ? 'dark' : 'light'" autoplay loop muted playsinline class="bg-video">
    <source :src="store.isDarkMode ? '/bg-dark.mp4' : '/bg-light.mp4'" type="video/mp4" />
  </video>
  
  <!-- 如果用户上传了自定义壁纸素材，则驱动这层渲染 -->
  <img v-else-if="store.bgType === 'image'" :src="store.bgUrl" class="bg-video" style="object-fit: cover;" />
  
  <!-- 核心光罩：在动态视频上方强制蒙上一层遮盖。如果是黑夜，则覆盖 60% 黑幕；日间是 10% 白慕 -->
  <div class="bg-overlay" :style="{ background: store.isDarkMode ? 'rgba(0,0,0,0.6)' : 'rgba(255,255,255,0.1)' }"></div>
</div>
```

### 2. 极致定制化：运行时状态驱动界面滤镜
如何实现用户自主调色？并不是简单的换两个 CSS 文件。您使用 `Vue3 v-bind` 将操作杆捕捉到的值，**通过 JS 实时注入给全局容器节点**的主体框架。
```javascript
// 【设定氛围色引擎】来自 settings 控制器
const setWindowTint = (color) => {
    store.windowTint = color; // 更新响应式状态池里的基准色
    applyThemeColor();        // 唤醒色彩渲染引擎重新构图 CSS 滤镜树
};

// 后台监听到通透度滑动条 (Slider) 被改变时
const handleOpacityChange = (e) => {
    store.glassOpacity = parseFloat(e.target.value); // 取值 0.1~0.95
    applyThemeColor();
};
```
而最神级的**自定义原生取色器**代码如下，将 HTML5 的原生 `<input type="color">` 做了透明隐藏，覆盖在一副七彩背景之上。用户点击彩色盘就会自然触发取色器！
```html
<label class="color-btn custom-color-picker" style="background: conic-gradient(red, yellow, lime, aqua, blue, magenta, red); position: relative; overflow: hidden;">
    <span style="color: white; z-index: 2; pointer-events: none;">+</span>
    <!-- 隐身的原生极客调色板，z-index置底且被隐藏了外壳，一触即发 -->
    <input type="color" @input="actions.handleCustomTint" style="opacity: 0; position: absolute; ...">
</label>
```

### 3. Glassmorphism CSS 毛玻璃渲染核心
整个系统的“毛玻感”实质上来源于 CSS3 的最新滤镜层层叠加：
```css
/* Sidebar.css 或 DataScreen.css 里的玻璃盒子抽象类 */
.glass-card {
  background: rgba(255, 255, 255, var(--glass-opacity)); /* 透明度跟随操作手柄呼吸 */
  backdrop-filter: blur(15px);                            /* 这个就是将底端视频完全“毛化”的渲染晶体引擎 */
  border: 1px solid rgba(255, 255, 255, 0.2);             /* 利用白色的极细边框营造出倒角的玻璃切割折射反光 */
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);             /* 投向底层屏幕的悬浮投影 */
}
```

---

## 💻 第二篇：全系统代码独立功能块解构

在这部分，我们将系统里前后端干活的文件结构，从上到下独立剥离解释。

### 1. `frontend/src/store.js` —— 系统的数据神经总枢
Vue 本来是组件和组件层层嵌套传值的（父传子，子传孙），这就好像是打电话要层层转交非常慢。你在这个文件建立了一个**“数据金字塔”**。
* **独立的底层状态记录**：这里只放所有的原始变量。
* **引入了 Mixin 动作黑科技**：由于要处理的功能极度庞杂，你把 `analysis`的、`intel`AI处理的、`process`流程的 JS 逻辑切分在了别的文件夹下面，最后使用 `Object.assign(actions, setupBase...)` 进行**动作拼接**！这种工业级的微模块解耦是极其优雅的工程思维范式。

### 2. `frontend/src/Sidebar.vue` —— 中央控制中继器
导航左侧面板不仅是按钮合集，更是路由分发中心。
每次用户点击（如：触发“描述统计”、“智能分析”），`Sidebar` 完全不做计算的脏活累活。**它只负责调用 `actions` 去操作 `store`。** 保证了系统的纯净与单一响应原则：
```html
<button @click="actions.runMachineLearning" class="glass-btn action-btn">
   ⚡ 训练随机森林模型
</button>
```

### 3. `backend/app.py` 配合代理网关 —— 分离式架构破除防壁
如果你去探究你的 Flask `app.py` 的架构，你会发现它完全没有掺杂如何吐出 HTML 文件的操作。
* **CORS 跨域资源共享**：配置了 `CORS(app)` ，允许来自 `Vite` 端口的前端 AJAX 的各种刁钻跨域穿刺请求。
* **蓝图架构 (Blueprints)**：`app.register_blueprint(process_bp)`。你的后端被拆分为了独立的“清洁处理”、“数据解析”、“图表渲染”服务节点蓝图。如果这家企业明天准备引入其他的应用功能线，只需要写个新蓝图插在上面，具备无限制横向扩容潜力。

### 4. 数据字典 CRUD 引擎块 (`DataEditor.vue` 手工编辑区)
在您的 `DataEditor.vue` 纯净表格组件里，如果用户要在海量数据里添加一行，或者剔除一列，您并不是把全部数据传给后端清洗一遍再拉取回来。而是在前端本地直接使用了 **DOM V8引擎级别自带的切分函数**。
```javascript
// 在二维矩阵的末尾推入一行空白列
const addGridCol = () => {
  store.manualGrid[0].push(`列 ${store.manualGrid[0].length + 1}`);
  // 因为是从索引 1 开始的，直接把空白值填充给所有行对应的结尾
  for (let i = 1; i < store.manualGrid.length; i++) {
    store.manualGrid[i].push("");
  }
}

// “截断性剔除”引擎。
const removeGridRow = (rIdx) => {
  // 利用原生数组切片 splice，定点且高速移除 1 个元素，拒绝耗时的深克隆。不会使得网页卡顿。
  store.manualGrid.splice(rIdx, 1);
}
```

### 5. `frontend/src/App.vue` —— 异步遮罩警告中心
系统非常贴心地把所有的“二次提示弹框”（比如果真的要覆盖某文件、智能清洗后的战报）**放在了系统的最外层**。
这保证了这些危险警报器的 `z-index` 可以突破哪怕是最外层的玻璃界面，实现全阻断效果：
```html
<!-- 如果监测到了冲突状态，弹出的模态框，z-index 高达 9999 -->
<div v-if="store.dialog.show" class="modal-overlay" style="z-index: 9999;">
  <div class="modal-content glass-card">
     <!-- 根据危险类型的不同，渲染颜色截然不同。黄灯警示或者蓝灯提示 -->
    <h2 :style="{ color: store.dialog.type === 'confirm' ? '#e6a23c' : '#409eff'... }">
```

### 6. EChart 全局销毁与阻断泄露块 (存在于 `DataScreen.vue`)
画完几百张图如果不及时“撕掉画布”，浏览器显卡会被数据大屏卡死。因此在图表组件中调用了重度保障机制。
只要前端检测到试图画一张全新的图表，会先用 `myChart.dispose()` 将旧挂载池里的缓存显存强行爆破回收，这也是您系统**极其顺滑且能长期在后台静默运行不崩溃的命脉代码所在**。
