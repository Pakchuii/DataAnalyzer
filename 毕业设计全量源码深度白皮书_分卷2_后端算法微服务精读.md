# 🎓 毕业设计万字终极源码解析方案（第二卷：后端算法与数据微服务）

> **导读**：本白皮书切分为四个分卷。这是**【第二卷】**，将系统后端中最能代表你理论水平的、由你亲自编纂的基于数学底层的智能算法引擎代码彻底“开膛破肚”，展现所有机器学习和统计算法的深层推演细节。

---

## 模块 4：三阶智能数据流清洗滤网与安全沙盒 (`backend/routes/process_routes.py`)
这是整个系统数据清洗的核心灵魂代码，不粗暴截取，而是凭借形态进行动态切割。

### 三阶段判定引擎：
```python
def smart_clean_series(s):
    # 第一步：把当前这个特征的所有数据抽取，并用 dropna 抛弃所有的 NaN 缺失坑洞，拿到净体。
    s_valid = s.dropna()
    n = len(s_valid)
    if n < 3:
        return s, "样本极小(跳过)", 0 
        
    # 【一阶段】：当 N(总数) < 30 的极小样本模式
    if n < 30:
        # 这个 q95 就是大名鼎鼎的统计学 Dixon's Q 表格中的 95% 置信度基准数组。
        q_crit = q95.get(n, 0.260)
        s_sorted = s_valid.sort_values().values
        s_clean = s.copy()
        
        # 算极差比例，通过把整个数组排头和排尾的值进行相切与总长池对比。
        # 一旦发现超出 q_crit ，判定此处必然形成异常断崖，立刻调用 .replace() 进行相近节点拉近取代。
        range_all = s_sorted[-1] - s_sorted[0]
        if range_all > 0 and (s_sorted[1] - s_sorted[0]) / range_all > q_crit:
            s_clean = s_clean.replace(s_sorted[0], s_sorted[1])
        # ... 后续进行最高值同样替换并 return

    else:
        # 【二阶段/三阶段判定点】：开启偏度 (Skewness) 雷达探测
        skewness = s_valid.skew()

        if pd.notna(skewness) and abs(skewness) <= 1.0:
            # 高斯准则：如果数据形态像个古钟一样左右平均。采取极度纯粹狠毒的数学定理切割
            mean_val = s_valid.mean()
            std_val = s_valid.std()
            # 求得均值下方 3倍标准差 和 均值上方 3倍标准差
            lower_bound = mean_val - 3 * std_val
            upper_bound = mean_val + 3 * std_val
            # 利用 pd.clip 一刀两断，溢出区域的死线全贴在墙根上
            s_clean = s.clip(lower=lower_bound, upper=upper_bound)
            return s_clean, "3σ边界拦截 (N>=30, 呈近似正态)"

        else:
            # 偏态准则：长尾分布特征严重（比如工资、成绩极值），如果用上面这一步直接把原本健康的偏态砍飞了。
            # 选择 IQR 距离非参数法作为缓冲带
            Q1, Q3 = s_valid.quantile(0.25), s_valid.quantile(0.75)
            IQR = Q3 - Q1
            # 取箱子长度乘以常数常量 1.5 延伸至四界边界底座。
            s_clean = s.clip(lower=Q1 - 1.5 * IQR, upper=Q3 + 1.5 * IQR)
            return s_clean, "IQR箱线图法 (N>=30, 呈偏态分布)"
```

### 数据库源沙盒机制与无污染落盘：
你所有的清理操作都不会直接感染本地数据文件。
```python
# 每次修改都在一个新的 Outputs 目录，除非...
@process_bp.route('/api/data/save', methods=['POST'])
def save_data():
    save_mode = request.json.get('save_mode', 'new_output') # 获取确认标记
    overwrite_confirmed = request.json.get('overwrite_confirmed', False)
    
    # 只要你的保护模式选择为 overwrite（强行写源文件）而又没做二次确认
    if save_mode == 'overwrite' and not overwrite_confirmed:
        return jsonify({"status": "exists", "message": "发现危险覆盖请求。确认覆盖？"})
    # 彻底解锁覆盖防护（比如在前端接受了点名），利用 os 开刀移除：
    if save_mode == 'overwrite':
        os.remove(old_filepath) 
        # 此处方允许使用新内存写入的 pd dataframe 做 .to_csv 回填
```

---

## 模块 5：机器学习的多重防伪与推荐 (`backend/services/service_ml.py`)
机器学习（Sklearn）虽然只要几行，但在该项目中给它穿满了几十行定制的铠甲。

### 靶点智能发现 (`do_summary`)：
```python
def do_summary():
    # ... 检测稀疏度和过拟合后 ...
    # 【特征推荐计算核心矩阵】
    # 将目前这个数据群各个数字维度的特征通过 corr() 皮尔逊相关性进行方阵联和。
    corr_df = numeric_df.corr().abs()
    # sum() 取它的重叠相关分数总和，然后 idxmax() 返回分数最大的那个列名字！
    # 这就是你系统的精华：能在杂乱如麻的数据里算出谁才是真正具备牵连作用的隐藏 Boss 变量！
    target_candidate = corr_df.sum().idxmax()
```

### 多重退化熔断算法 (`do_predict_new`)：
不要让模型用 $R^2$ 说瞎话。
```python
def do_predict_new():
    # 核心：调集之前从 /api/predict 接口训练出来并在内存中保持的 Random Forest 模型
    y_show_pred = model.predict(X_show) 

    sample_r2 = r2_score(y_show_real, y_show_pred) # 常规的准确比
    # 抽出预测曲线和现实拟合曲线交错在一起的物理空间贴合程度：
    corr = np.corrcoef(y_show_real, y_show_pred)[0, 1] if not (np.std(y_show_pred)==0 or np.std(y_show_real)==0) else 0

    # 让预测数值绝对偏离距离映射为准确率：加 1e-9 锁死分母变 0
    mape = np.mean(np.abs((y_show_real - y_show_pred) / (y_show_real + 1e-9)))
    acc = max(0, 1 - mape)

    # 究极断头惩罚台：
    # 即便 r2_score 算出来因为数据散落被欺骗了，如果发现两组线形神离合完全不搭 (corr < 0.3)，
    # 或已经到了无药可治的地步，直接抹去其高分：
    if sample_r2 < 0.15 or corr < 0.3:
        confidence = round(acc * 45, 2)
    else:
        # 常态下允许准确度占40比重，曲线走势重叠占60比重。打分！
        confidence = round((acc * 0.4 + corr * 0.6) * 100, 2)
```

---

## 模块 6：统计核心逻辑与极客级防暴毙引擎 (`backend/services/service_stats.py`)
系统面对不正常的数组不报错，而是施展极客手法进行“软抢救”。

### Bootstrap 双重拦截重塑 (`do_ttest`)：
```python
def do_ttest():
    # 抽出了双边队列 d1, d2
    equal_var_assumption = False # T 检验最怕方差不齐，默认关闭，转而启用 Welch's T Test 以防止报错。

    # 如果一组抽样竟然不到中心定律规定的 30 条！
    if n1 < 30 or n2 < 30:
        # 这里用夏皮罗算法做正态检测，要求至少 3 条以上。
        p_shap1 = stats.shapiro(d1)[1] if n1 >= 3 else 0
        p_shap2 = stats.shapiro(d2)[1] if n2 >= 3 else 0
        
        # 极差的样本如果碰见 P<0.05 证明已经极度长尾歪斜分布了
        if p_shap1 < 0.05 or p_shap2 < 0.05:
            # 将他们强行扯下桌子！调用 numpy 运用有放回的规则抽取 100 遍生成全新平行分身群组。
            # 这 100 次抽样后重组的数据群方差将极度磨平，成功过渡至常态正态曲面。
            d1 = np.random.choice(d1, size=100, replace=True)
            d2 = np.random.choice(d2, size=100, replace=True)
            algorithm_note = "数据非正态, 已触发 Bootstrap 重抽样(N=100)"
            
    # 【底层核心计算】：安全通过上述修补防线的数据终汇合于此，通过 ttest_ind 求出判词。
    stat, p_value = stats.ttest_ind(d1, d2, equal_var=equal_var_assumption)
```

---

## 模块 7：图表数据空间降维与不可退隐私处理
前端能保证 60fps 不卡顿，核心在后端传回来的数据做了一层高效率的封装 (`backend/services/service_visualize.py` & `security`)。

### 饼图与雷达图极速预清理：
```python
def do_categorical():
    # 你在渲染分布占比图的时候不是全画：
    # 如果检测到这种所谓的不重复的数据（比如每个人的身份证号），超过 15 种。
    # 把类别型超过 15 个独立特征的给剔除了，以免画出一个 200 片全挤在一起恶心死人的马赛克饼图。
    if df[col].nunique() > 15: continue
```
```python
def do_radar():
    # 这是防止雷达图最高点触顶导致最外面那一圈多边形突破浏览器边框的绝顶妙招：
    # 将所有的极限值全部乘以 1.1 的预留边距进行空间扩展
    indicators = [{"name": col, "max": float(numeric_df[col].max()) * 1.1} for col in valid_numeric_cols]
```

### 直流数据安全抹除 (`do_mask` / `service_security.py`)：
```python
def mask_val(x):
    # 这就是你独家发明的“长度感知识别法”。
    # 它不会像一般的屏蔽一样不管长短全给你变成 ****** 无法直视。
    if len(x) <= 2: return x[0] + '*'               # 只遮最后
    elif len(x) == 3: return x[0] + '*' + x[-1]     # 首尾不掩
    else: return x[:4] + '****'                     # 长身份证从第七号起粉碎

# 遍历时扫描任何属于 "被污染可能性" 序列的名字，全面执行洗白过滤网 .apply(mask_val)
if any(kw in str(col).lower() for kw in ['名', 'name', '手机', '身份证']):
    df[col] = df[col].apply(mask_val) 
```

> **卷二小结**：通过细抠后端这些不为人知的黑武器，你完全有底气向评委宣称：“我这不是一个调包侠作品，而是一整套严密监控数学容错性、统计显著性崩盘与过拟合缺陷的工业化数据推演核心。”
