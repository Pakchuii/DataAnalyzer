# 🧠 后端核心引擎体系与统计算法深度解析专报

之前的文档侧重于业务流和架构，本文档则**完全聚焦于系统后端的“数学心智”与“算法实现”**。
为了打破传统软件单纯封装第三方包的弊病，本系统基于 `Pandas`、`Scipy`、`Scikit-learn` 等底层库，深度重构了一套容错率极高的统计与机器学习处理流。以下是全核心算法的深度解析：

---

## 🔮 模块一：机器学习深度探针与预测模型引掣 (`service_ml.py`)

### 1. 结构化数据宏观质量探针 (`do_summary`)
普通的分析系统只会简单输出数据名，我们的后台写了一个能自动出具“体检报告”的 AI 前置探针。
* **稀疏度计算与过拟合预防**：
  后台首先通过 `df.isnull().sum().sum() / total_cells` 计算全局矩阵稀疏度。一旦发现样本量不足 (`< 30`) 就会强行警告用户可能有严重的“过拟合边界”。当发现稀疏度 `> 0.15` (即有超过15%的空洞) 时，提示调用清洗引擎阻断噪音传入。
* **靶点特征自发现算法 (Auto-Target Recommendation)**：
  对小白用户来说，他们不懂选谁来做被预测的目标变量（Y树）。系统在后台计算全部数值特征的 Pearson 相关系数总绝对矩阵，随后执行：
  `target_candidate = corr_df.sum().idxmax()`
  将与数据集中其他变量**联动性总权重最高**的那一个特征自动抓出，并推荐其作为决策靶点！

### 2. 多重衰减式启罚模型置信度推导 (`do_predict_new`)
传统的分类回归任务（如 Random Forest）最后仅仅依赖 $R^2$ (决定系数)。但是 $R^2$ 在样本极度弥散时容易骗人。
在这个模块，模型对用户的仿真下发不仅仅传出了随机森林执行结果，还自创了复杂的**结果防伪惩罚算法**：
```python
# 1. 采集模型的基础误差
sample_r2 = r2_score(y_show_real, y_show_pred)
# 2. 捕捉预测曲线和真实波形的“物理相位相似度”
corr = np.corrcoef(y_show_real, y_show_pred)[0, 1] 
# 3. 得到真实误差占比平均化 (MAPE)
mape = np.mean(np.abs((y_show_real - y_show_pred) / (y_show_real + 1e-9)))

# 核心判决层：系统认为如果 R2 指数极低(<0.15)或者两条线的趋势背道而驰 (横切协方差 corr<0.3)
if sample_r2 < 0.15 or corr < 0.3:
    # 触发【极刑惩罚】：强制压将模型置信度锁死在极限 45 下方。
    confidence = round(max(0, 1 - mape) * 45, 2)
else:
    # 正常融合置信度模式，权重上曲线相似度占导向比例 (0.6)
    confidence = round((max(0, 1 - mape) * 0.4 + corr * 0.6) * 100, 2)
```

---

## 📐 模块二：极富弹性的高级统计框架 (`service_stats.py`)

传统统计学最大的问题在于：假设要求苛刻。比如 t 检验要求极高的方差齐性。如果不满足即报错。本系统的后台处理中运用了强拦截与重抽样。

### 1. 描述与探索性分析 (`do_advanced`)
除了下发常见五数概括（最大最小中位等），底层通过 `scipy.stats.shapiro(d)` 直接运行基于夏皮罗方法的**非参检验法**来扫描数据的形态。因为只要 $p>0.05$ 这个金标准一旦达成，数据就被证实呈正态分布曲线，就为上层其他统计手段开了绿灯。

### 2. t检验双漏斗拦截与 Bootstrap 强效救援 (`do_ttest`)
如果两组数据的方差不齐怎么办？后台默认挂载了韦尔奇适配：`ttest_ind(d1, d2, equal_var=False)`，直接用更加宽容的 Welchs t检验硬扛方差差异。
那如果样本太少（连正态都不符合）怎么办呢？
```python
if n1 < 30 or n2 < 30: # 当有一组阵列小于中心极限定律标准边缘
    p_shap = stats.shapiro(d)[1]
    if p_shap < 0.05: # 如果检验 P 值宣告破产，证明源数据极度偏倚
        # 激活大数定理的终极救援手段：100 次替换重放回式 Bootstrap
        # 从原本可能扭曲的小数据集中，有放回地抽出 100 个模拟新群，借以抵消掉偶发极端造成的干扰
        d1 = np.random.choice(d1, size=100, replace=True)
```
后台巧妙借用 Bootstrap 平滑了所有无法通过门槛的特殊病例，赋予了平台统计功能的极强泛化承载力。

---

## 🧹 模块三：三阶非线性数据保洁滤网 (`process_routes.py`)

清洗往往面对的都是极其庞大脏乱的 CSV 字符阵，这层相当于系统的胃功能，负责全盘消化异常。如果粗暴采用去头切尾极易切割有效数据。它根据数据自己的体态实行路由切割。

### 1. N < 30 的小样本：Dixon's Q 断崖判别池
如果是小样本，3倍标准差早就偏到天上去了。所以在代码里采用了内置的哈罗氏常量表 (`q95 = {3: 0.941, 4: 0.765 ... 30: 0.260}`)：
利用排序后的极差比例： `(X2 - X1) / (Max - Min)`去和静态常数映射字典做对抗衡量。一旦查验到某一个孤岛数据造成断崖式拉高，直接以临近点坐标将它覆盖填平替换(`clip`)。

### 2. N >= 30 且形态均衡：高速 $\mu \pm 3\sigma$ 高斯围墙 
对于大量中心聚类的数值型向量组，首先执行偏度校验探测 `abs(skewness) <= 1.0`。
确定对称后，采用极限矩阵切分 `s_clean = s.clip(mean - 3*std, mean + 3*std)` 把溢出的高频数据完全压缩封锁在高斯钟形罩底部。

### 3. N >= 30 且重型右偏/左偏：高阶抗扰动 IQR 非参切刀
有些数据天生就是长尾的（比如全班得分低靡但有个别天才）。这导致其自身的平均值其实已被拉离中环。
因此，这里使用强抗干涉的**数据排序分位点**进行切割操作：
```python
Q1 = s_valid.quantile(0.25) # 切出 25% 基线点
Q3 = s_valid.quantile(0.75) # 切出 75% 高压点
IQR = Q3 - Q1               # 构建箱线图实体矩宽 
# 利用四分位区间乘以一个常数 1.5 构建上下的抗扰动胡须边界，实现软切片。
lower_limit, upper_limit = Q1 - 1.5 * IQR, Q3 + 1.5 * IQR 
```

### 4. 数据标准化去量纲 ($Z$-Score) 的数学赋能 
如果在同一个表里面有一列是薪资“千万级别”，有一列是年龄“十位数级别”，一旦送入深度学习就会发生极端权重偏倚崩盘。
API 层直接利用矩阵化引擎 `(df[col] - df[col].mean()) / df[col].std()` 完成降伏，将所有杂乱的单位空间缩放到以 0 为期望，1 为标准差的最正统矩阵空间内。

---

## 🔐 模块四：物理沙箱与内存清洗引擎 (`process_routes.py`)

处理表单意味着会读出几百 M 存放在服务器内存的数据字典。而且由于分析不断发生，后台会不断吐入不同的 `Outputs` 输出包。
所以数据保护和磁盘冗余处理在这里同样展现为坚实的构筑代码：

### 1. Overwrite Confirmed 二阶段沙盒阻塞
所有的清洗或者生成图的操作，都不会触碰 `uploads/filename.csv` 文件原品，而是会被转移克隆到 `outputs/` 输出沙盒，建立新的指针文件。
只有等客户端发来 `save_mode == 'rename_source'` 这种强行替代参数时。底层才会解开确认锁执行 `os.remove()`。

### 2. PII 反洗白不可逆混淆
在安全 `service` 层内，代码利用类似于正则但更快速的向量生成模式检测字段。
针对截获的包含"手机、身份证"的关键列，实施长度敏感型降维。只要长于4位，一律截断 `x[:4] + '****'`。这彻底从物理根源上粉碎了逆向工程的解码企图，实现完美的医疗/表单等合规性保护。
