# 👨‍🏫 答辩专用：核心逻辑流程速记白皮书

> **提示：** 这份速记专门为您在向评委解释项目核心逻辑时（尤其是在演示系统或面对评委提问时）提供**脱稿级的“大白话+技术专有名词”**话术。

---

## 一、 数据智能清洗的 3 个判断路口（当评委问：你的清洗怎么做的？）
**【讲解脉络】**
“评委老师您好，面对脏数据，我们系统不是死板的一刀切，而是根据数据的形态特征实现**三级自适应切分**的：”
1. **路口一（极小样本探测）**：后台读到样本量 `N < 30` 时，系统自动跑 `Dixon's Q` 检验进行邻近截断，因为小样本方差大，传统方法容易误杀。
2. **路口二（近似正态探测）**：如果 `N ≥ 30`，系统会立刻算一个“偏度（Skewness）”。如果偏度绝对值小于 1，说明数据是对称的、健康的。此时激活数学效率极高的**正态 3σ 边界准则**来拦截异常低谷和高峰。
3. **路口三（严重偏态探测）**：如果偏度大于 1（严重左偏或右偏），强行用公式会把正常数据砍断。此时系统会路由到非参数的 **IQR 四分位箱线图算法**，依靠 25% 到 75% 的极值中位宽距来温和收敛异常值。

## 二、 隐私护城河与导出防呆（当评委问：系统安全性怎么保证？）
**【讲解脉络】**
“我是从一头一尾两个关卡拦截风险的。”
1. **上传端智能脱敏**：用户一传表格，后端会用字符串 `lower()` 去嗅探特征名，一旦碰到“name、手机、身份证”就会触发清洗。并根据字符长短进行**不可逆的打码算法**。（比如短名遮中，长号遮4位）。
2. **导出端文件沙盒**：编辑完数据如果直接出库很容易把原始心血冲掉。前端设置了 `Overwrite Confirmed` 多重异步拦截；写入时隔离在后端的 `outputs` 沙盒文件夹中。最后下发 CSV 时，代码硬塞了一个 `\uFEFF` UTF-8 BOM，彻底解决 Windows Excel 导入文件乱码的世界难题。

## 三、 T检验的 Bootstrap 补救与预测系统的惩罚（当评委问：为什么你的统计更稳定？）
**【讲解脉络】**
“本系统最大的工程学创新，就在于防爆与兜底机制。”
1. **Bootstrap 的 T检验兜底**：T检验要求数据正态，普通软件一旦发现数据不正态就直接报错假死。我的代码会前置跑一下 `Shapiro-Wilk` 正态检验。一旦它报错，底层会隐秘地触发“大数定理”，**默认开启 100 次 `size=100` 的有放回 Bootstrap 数据增强抽样**，强行把数据掰成正态分布，保障后续流程不崩溃。
2. **机器学习的“反诈”惩罚**：随机森林出的 R² 分数有时看似很高，实则是纯均值（也就是预测变成了一条死直线）。因此我构建了 `(误差补偿 × 0.4) + (Pearson相切追踪 × 0.6)` 的算法。如果发现曲线形变（相关系数降到 0.3 以下），系统会触发熔断惩罚，强行把置信度压低于 45 分！坚决阻断劣态模型去误导用户。

## 四、 极简 UI 设计与状态管控（当评委问：前端交互有何亮点？）
**【讲解脉络】**
1. **极致 Glassmorphism (毛玻璃) 显示**：为了让枯燥的数据分析具有科技感，我摒弃了白色看板，选用了 HTML5 动态视频作为渲染底层，上面覆盖了一层 `backdrop-filter: blur(x)` 的 CSS。
2. **主题引擎与状态栈**：
   - 使用了 Vue3 的 **响应式状态树 Pinia 替代传统级联组件寻址**。
   - 自主首创了针对大表单编辑的 **Memento (备忘录) 模式撤销层**。设定了 30 层数据硬限制，超过自动丢弃尾栈元素（`shift`），这避免了传统记录会导致系统内存极速溢出（Out Of Memory）的问题。
